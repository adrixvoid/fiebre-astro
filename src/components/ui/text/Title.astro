---
import { titleVariants, type PropsTitle } from "./Text.cva";

const {
  as: Element = "h1",
  class: className = "",
  align,
  variant,
  size,
  bold,
  fontStyle,
  baseline,
  ...props
} = Astro.props as PropsTitle;
---

<Element
  class={titleVariants({
    variant,
    size,
    align,
    bold,
    fontStyle,
    baseline,
    className,
  })}
  {...props}
>
  <slot />
</Element>
