---
import { textVariants, type PropsParagraph } from "./Text.cva.ts";

const {
  class: className = "",
  align,
  variant,
  size,
  as: Component = "p",
  fontStyle,
  ...props
} = Astro.props as PropsParagraph;
---

<Component
  class={textVariants({ variant, size, align, fontStyle, className })}
  {...props}
>
  <slot />
</Component>
