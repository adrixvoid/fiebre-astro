---
import { cvaVariants, type PropsParagraph } from "./Text.cva.ts";

const {
  class: className = "",
  align,
  variant,
  size,
  as: Component = "p",
  fontStyle,
  ...props
} = Astro.props as PropsParagraph;
---

<Component
  class={cvaVariants({ variant, size, align, fontStyle, className })}
  {...props}
>
  <slot />
</Component>
